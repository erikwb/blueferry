# Fedora installs bluetoothd below /usr/libexec. Exposing the experimental
# interfaces is required for BlueZ's per-transport bearer API.
[Service]
ExecStart=
ExecStart=/usr/libexec/bluetooth/bluetoothd -E
