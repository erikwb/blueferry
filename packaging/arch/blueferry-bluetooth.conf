# Expose BlueZ's per-transport Bearer.LE1/Bearer.BREDR1 interfaces and the
# Device1.PreferredBearer property. BlueFerry uses these to keep ANCS on LE
# while MAP/PBAP remain connected over BR/EDR.
[Service]
ExecStart=
ExecStart=/usr/lib/bluetooth/bluetoothd -E
